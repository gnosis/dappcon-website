import React from 'react'
import styled from 'styled-components'
import { colors } from 'theme'

const Wrapper = styled.div`
    height: 100vh;
    background-color: ${colors.reddishPink};
`

const MainSection = () => <Wrapper />

export default MainSection